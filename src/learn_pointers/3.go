/* package main

import "fmt"

func main() {

	// Pointers
	//var a int
	//var b int
	//b = &a
	/*
			po := new(int)
			fmt.Printf("value as %v is %v", po, *po)

		a := 5
		Double(&a)
		fmt.Println(a)
		b := [4]int{1, 2, 3, 4}  // since this is array that is why - see func Doublearray
		Doublearray(&b)
		fmt.Println(b)
*/
/*
}
*/
/*
func Double(p *int) {
	*p = *p * 2
}

func Doublearray(p *[4]int) {
	for i := range *p {
		//(*p)[i] = (*p)[i] * 2   // such type of indexing can happen with slice as well as array.
		p[0] = 0   // such type of indexing can happen with array not with slice

	}
}
*/
